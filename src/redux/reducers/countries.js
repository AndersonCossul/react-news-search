import * as actions from '../actions/actions'

const initialState = {
  autocomplete_contries: []
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer